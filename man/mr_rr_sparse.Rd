% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr-rr-sparse.R
\name{mr_rr_sparse}
\alias{mr_rr_sparse}
\title{Sparse reduced-rank Mendelian randomization estimator}
\usage{
mr_rr_sparse(
  Y,
  X,
  r,
  Sigma_X,
  lambda = rep(0.001, ncol(X)),
  W = NULL,
  max_iter = 100L,
  tol = 0.01,
  threshold = 0.01,
  solver = "OSQP"
)
}
\arguments{
\item{Y}{A numeric matrix with one row per genetic instrument and one column
per outcome.}

\item{X}{A numeric matrix with one row per genetic instrument and one column
per exposure.}

\item{r}{A positive integer specifying the working rank of the causal effect
matrix.}

\item{Sigma_X}{A symmetric matrix containing the average measurement-error
covariance of the SNP-exposure association estimates.}

\item{lambda}{A non-negative number or a numeric vector of length
\code{ncol(X)}. A scalar is recycled across exposures.}

\item{W}{An optional positive-definite outcome weight matrix. If \code{NULL}, the
identity matrix is used.}

\item{max_iter}{A positive integer giving the maximum number of alternating
optimization iterations.}

\item{tol}{A positive number used as the relative convergence tolerance.}

\item{threshold}{A non-negative number. Entries of the final coefficient
matrix with absolute value below this threshold are set to zero. The
default, \code{1e-2}, matches the frozen paper implementation.}

\item{solver}{A single character string naming a CVXR solver. The default is
\code{"OSQP"}.}
}
\value{
A list containing:
\describe{
\item{A}{An outcome loading matrix with dimensions \code{ncol(Y)} by \code{r}.}
\item{B}{The thresholded exposure coefficient matrix with dimensions
\code{r} by \code{ncol(X)}.}
\item{AB}{The thresholded causal effect matrix \verb{A \\\%*\\\% B}.}
\item{B_raw}{The exposure coefficient matrix before thresholding.}
\item{AB_raw}{The causal effect matrix before thresholding.}
\item{iter}{The number of alternating optimization iterations performed.}
\item{dist}{The final relative convergence distance.}
\item{converged}{Whether \code{dist} was smaller than \code{tol}.}
}
}
\description{
Estimates a low-rank causal effect matrix while applying column-specific
L1 penalties to the exposure coefficient matrix.
}
\details{
\code{lambda} controls the optimization penalty, whereas \code{threshold} is applied
only after the alternating optimization has finished. The unthresholded
estimates are retained in \code{B_raw} and \code{AB_raw}.
}
