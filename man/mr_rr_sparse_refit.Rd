% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr-rr-sparse-refit.R
\name{mr_rr_sparse_refit}
\alias{mr_rr_sparse_refit}
\title{Post-selection refit for sparse reduced-rank MR}
\usage{
mr_rr_sparse_refit(
  Y,
  X,
  Sigma_X,
  support,
  W = NULL,
  A_init = NULL,
  B_init = NULL,
  max_iter = 100L,
  tol = 0.01
)
}
\arguments{
\item{Y}{A numeric matrix with one row per genetic instrument and one column
per outcome.}

\item{X}{A numeric matrix with one row per genetic instrument and one column
per exposure.}

\item{Sigma_X}{A symmetric matrix containing the average measurement-error
covariance of the SNP-exposure association estimates.}

\item{support}{A logical or binary numeric matrix with dimensions \code{r} by
\code{ncol(X)}. Nonzero entries identify the coefficients of \code{B} that are
included in the refit.}

\item{W}{An optional positive-definite outcome weight matrix. If \code{NULL}, the
identity matrix is used.}

\item{A_init}{An optional numeric matrix with dimensions \code{ncol(Y)} by \code{r}
used to initialize \code{A}. \code{A_init} and \code{B_init} must either both be supplied
or both be \code{NULL}.}

\item{B_init}{An optional numeric matrix with dimensions \code{r} by \code{ncol(X)}
used together with \code{A_init}. Entries outside \code{support} are set to zero.}

\item{max_iter}{A positive integer giving the maximum number of alternating
optimization iterations.}

\item{tol}{A positive number used as the relative convergence tolerance.}
}
\value{
A list containing:
\describe{
\item{A}{An outcome loading matrix with dimensions \code{ncol(Y)} by \code{r}.}
\item{B}{A refitted exposure coefficient matrix with dimensions \code{r} by
\code{ncol(X)}, with exact zeros outside \code{support}.}
\item{AB}{The refitted causal effect matrix \verb{A \\\%*\\\% B}.}
\item{support}{The logical support matrix used in the refit.}
\item{iter}{The number of alternating optimization iterations performed.}
\item{dist}{The final relative convergence distance.}
\item{converged}{Whether \code{dist} was smaller than \code{tol}.}
}
}
\description{
Refits a sparse reduced-rank Mendelian randomization model on a fixed
support without the L1 penalty used during support selection.
}
\details{
The support is held fixed throughout the refit. Conditional on this support,
the function alternates between an unpenalized least-squares update of \code{B}
and the same weighted Procrustes update of \code{A} used by \code{mr_rr_sparse()}.

For the paper analysis, \code{support} is selected once on the original data and
then held fixed across bootstrap samples. A typical support is
\code{sparse_fit$B != 0}.
}
