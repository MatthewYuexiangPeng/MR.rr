% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-mr-rr-data.R
\name{prepare_mr_rr_data}
\alias{prepare_mr_rr_data}
\title{Prepare summary-level data for MR-rr estimators}
\usage{
prepare_mr_rr_data(
  beta_exposure,
  se_exposure,
  beta_outcome,
  se_outcome,
  cor_exposure = NULL,
  cor_outcome = NULL,
  allele_frequency = NULL,
  variant_variance = NULL,
  W = NULL
)
}
\arguments{
\item{beta_exposure}{A numeric matrix with one row per instrument and one
column per exposure.}

\item{se_exposure}{A positive numeric matrix with the same dimensions and
ordering as \code{beta_exposure}.}

\item{beta_outcome}{A numeric matrix with one row per instrument and one
column per outcome.}

\item{se_outcome}{A positive numeric matrix with the same dimensions and
ordering as \code{beta_outcome}.}

\item{cor_exposure}{An optional exposure-trait correlation matrix. The
identity matrix is used when this argument is \code{NULL}.}

\item{cor_outcome}{An optional outcome-trait correlation matrix. The
identity matrix is used when this argument is \code{NULL}.}

\item{allele_frequency}{An optional numeric vector with one effect-allele
frequency or minor-allele frequency per instrument. Values must be
strictly between zero and one. Under Hardy-Weinberg equilibrium and
additive genotype coding, the function calculates the variant variance as
\code{2 * allele_frequency * (1 - allele_frequency)}.}

\item{variant_variance}{An optional positive numeric vector containing one
genotype variance per instrument. This provides a direct alternative to
\code{allele_frequency}; the two arguments cannot be supplied together.
When either argument is supplied, all four association and standard-error
matrices are multiplied rowwise by \code{sqrt(variant_variance)}.}

\item{W}{An optional positive-definite outcome weight matrix. When \code{NULL},
the function uses the inverse of the constructed outcome measurement-error
covariance matrix.}
}
\value{
An object of class \code{mr_rr_data}, represented by a list containing
\code{X}, \code{Y}, \code{se_X}, \code{se_Y}, \code{Sigma_X}, \code{Sigma_Y}, \code{W}, \code{cor_X}, \code{cor_Y},
\code{allele_frequency}, \code{variant_variance}, and dimension metadata.
}
\description{
Validates aligned summary-association matrices and constructs the average
exposure and outcome measurement-error covariance matrices used by MR-rr.
}
\details{
All four input matrices must contain the same instruments in the same row
order and must already be harmonized to the same effect alleles. This
function does not perform allele harmonization, LD clumping, or instrument
selection.

The average exposure covariance is
\verb{Sigma_X = mean_j(D_Xj cor_exposure D_Xj)}, where \code{D_Xj} is the diagonal
matrix of exposure standard errors for instrument \code{j}. \code{Sigma_Y} is
constructed analogously from the outcome standard errors.
}
