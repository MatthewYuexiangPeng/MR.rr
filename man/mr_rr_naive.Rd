% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr-rr-naive.R
\name{mr_rr_naive}
\alias{mr_rr_naive}
\title{Naive Reduced-Rank Mendelian Randomization}
\usage{
mr_rr_naive(Y, X, r, W = NULL, W_inv = NULL)
}
\arguments{
\item{Y}{A numeric matrix with one row per genetic instrument and one column
per outcome.}

\item{X}{A numeric matrix with one row per genetic instrument and one column
per exposure.}

\item{r}{A positive integer specifying the working rank of the causal effect
matrix.}

\item{W}{An optional positive-definite outcome weight matrix. If \code{NULL}, the
identity matrix is used.}

\item{W_inv}{An optional precomputed inverse of \code{W}. This argument can reduce
repeated matrix inversion when both matrices are already available.}
}
\value{
A named list containing:
\describe{
\item{A}{The outcome loading matrix.}
\item{B}{The exposure loading matrix.}
\item{AB}{The estimated causal effect matrix.}
}
}
\description{
Estimates a rank-constrained causal effect matrix without correcting for
measurement error in the estimated SNP-exposure associations.
}
\examples{
X <- matrix(
  c(
    0.2, -0.1,
    0.5,  0.3,
   -0.4,  0.6,
    0.7, -0.5
  ),
  nrow = 4,
  byrow = TRUE
)

Y <- matrix(
  c(
    0.1,  0.3,
    0.4, -0.2,
   -0.3,  0.5,
    0.6, -0.4
  ),
  nrow = 4,
  byrow = TRUE
)

fit <- mr_rr_naive(Y = Y, X = X, r = 1)
fit$AB

}
