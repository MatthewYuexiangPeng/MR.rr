% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-all-mr-rr.R
\name{fit_all_mr_rr}
\alias{fit_all_mr_rr}
\title{Fit multiple MR-rr estimators to the same prepared data}
\usage{
fit_all_mr_rr(
  data,
  methods = c("naive", "corrected", "regularized", "sparse"),
  rank = NULL,
  rank_alpha = 0.05,
  rank_min = 1L,
  regularization_rate = 1e-13,
  regularized_implementation = c("spectral", "legacy"),
  sparse_lambda = 0.001,
  sparse_refit = TRUE,
  sparse_threshold = 0.01,
  sparse_max_iter = 100L,
  sparse_tol = 0.01,
  sparse_solver = "OSQP"
)
}
\arguments{
\item{data}{An \code{mr_rr_data} object created by \code{\link[=prepare_mr_rr_data]{prepare_mr_rr_data()}}.}

\item{methods}{A non-empty character vector containing any of \code{"naive"},
\code{"corrected"}, \code{"regularized"}, and \code{"sparse"}. Each method may appear
at most once.}

\item{rank}{A positive integer giving the working rank. When \code{NULL}, the
rank is selected by \code{\link[=select_mr_rr_rank]{select_mr_rr_rank()}}.}

\item{rank_alpha}{The sequential testing level passed to
\code{\link[=select_mr_rr_rank]{select_mr_rr_rank()}} when \code{rank} is \code{NULL}.}

\item{rank_min}{The smallest candidate rank considered by
\code{\link[=select_mr_rr_rank]{select_mr_rr_rank()}} when \code{rank} is \code{NULL}. It must be at least one
because the package estimators require a positive rank.}

\item{regularization_rate}{A non-negative regularization rate passed to
\code{\link[=mr_rr_regularized]{mr_rr_regularized()}}.}

\item{regularized_implementation}{Either \code{"spectral"} or \code{"legacy"}.
The default uses the numerically stable spectral implementation.}

\item{sparse_lambda}{A non-negative scalar or exposure-specific vector
passed as \code{lambda} to \code{\link[=mr_rr_sparse]{mr_rr_sparse()}}.}

\item{sparse_refit}{A logical value. If \code{TRUE}, sparse estimation consists
of penalized support selection followed by \code{\link[=mr_rr_sparse_refit]{mr_rr_sparse_refit()}} on the
selected support. The selection fit is retained in the returned object.}

\item{sparse_threshold}{A non-negative coefficient threshold passed to
\code{\link[=mr_rr_sparse]{mr_rr_sparse()}}.}

\item{sparse_max_iter}{The maximum number of alternating iterations used by
sparse selection and refitting.}

\item{sparse_tol}{The convergence tolerance used by sparse selection and
refitting.}

\item{sparse_solver}{The CVXR solver used by \code{\link[=mr_rr_sparse]{mr_rr_sparse()}}.}
}
\value{
An object of class \code{mr_rr_fit_collection}. Requested estimator fits
are available directly by method name, for example \code{result$corrected} and
\code{result$sparse}. The object also contains:
\itemize{
\item \code{methods}: The fitted methods in their requested order.
\item \code{rank}: The common working rank.
\item \code{rank_selected}: Whether the rank was selected automatically.
\item \code{rank_selection}: The shared rank-selection result, or \code{NULL} when rank
was supplied.
\item \code{data}: The prepared \code{mr_rr_data} object.
\item \code{call}: The matched function call.
}
}
\description{
Runs a selected collection of MR-rr estimators using one prepared dataset
and one common working rank. When the rank is not supplied, rank selection
is performed once and the selected rank is reused by every estimator.
}
\details{
This function is a batch interface around \code{\link[=fit_mr_rr]{fit_mr_rr()}}. It does not
implement separate estimator algorithms. Consequently, method-specific
results and controls have the same structure as the corresponding
\code{\link[=fit_mr_rr]{fit_mr_rr()}} result.

If \code{rank} is \code{NULL}, the first requested fit performs rank selection. The
resulting rank and rank-selection object are then attached to every fit in
the collection, so all estimators are directly comparable at the same rank.
}
\examples{
\dontrun{
fits <- fit_all_mr_rr(
  data = prepared,
  rank = NULL,
  regularization_rate = 1e-13,
  sparse_lambda = 1e-3
)

fits$corrected$AB
fits$regularized$AB
fits$sparse$AB
}

}
