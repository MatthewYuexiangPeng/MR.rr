% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr-rr-regularized.R
\name{mr_rr_regularized}
\alias{mr_rr_regularized}
\title{Spectrally regularized reduced-rank MR estimator}
\usage{
mr_rr_regularized(
  Y,
  X,
  r,
  Sigma_X,
  regularization_rate = 1e-13,
  W = NULL,
  W_inv = NULL,
  implementation = c("spectral", "legacy")
)
}
\arguments{
\item{Y}{A numeric matrix with one row per genetic instrument and one column
per outcome.}

\item{X}{A numeric matrix with one row per genetic instrument and one column
per exposure.}

\item{r}{A positive integer specifying the working rank of the causal effect
matrix.}

\item{Sigma_X}{A symmetric matrix containing the average measurement-error
covariance of the SNP-exposure association estimates.}

\item{regularization_rate}{A single non-negative number controlling the
amount of spectral regularization.}

\item{W}{An optional positive-definite outcome weight matrix. If \code{NULL}, the
identity matrix is used.}

\item{W_inv}{An optional precomputed inverse of \code{W}.}

\item{implementation}{Numerical implementation used to evaluate the
regularized inverse. \code{"spectral"} uses the stable eigenspectral formula
and is the default. \code{"legacy"} reproduces the implementation used for
the paper analyses.}
}
\value{
A list containing:
\describe{
\item{A}{An outcome loading matrix with dimensions \code{ncol(Y)} by \code{r}.}
\item{B}{An exposure coefficient matrix with dimensions \code{r} by \code{ncol(X)}.}
\item{AB}{The estimated causal effect matrix \code{A \%*\% B}.}
}
}
\description{
Estimates a low-rank causal effect matrix after correcting the exposure
second-moment matrix for measurement error and applying spectral
regularization.
}
